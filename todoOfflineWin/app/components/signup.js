import React from 'react';
import { Text, TouchableHighlight, StyleSheet } from 'react-native';

const SignUp = ({ border, title, selected, setType, type }) => (
        <Text >
            signup
        </Text>
);

export default SignUp;