import React from 'react';
import { Text, TextInput, TouchableHighlight, StyleSheet } from 'react-native';

const Login = ({ border, title, selected, setType, type }) => (
        <View>
            <header />
            <Text >
                login
            </Text>
        </View>
);

export default Login;